version: '3.9'

networks:
  trainee_network:
    driver: bridge

services:
  testapp:
    build:
      context: .
      dockerfile: ./srv/app/Dockerfile
    volumes:
      - .:/app
    networks:
      - trainee_network